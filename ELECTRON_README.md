# 🖥️ Desktop додаток Турнір

Нативний desktop додаток для Mac і Windows на базі Electron.

---

## 🚀 Розробка

### Запуск в режимі розробки:

```bash
npm run electron:dev
```

Це запустить:
1. Backend сервер (port 3000)
2. Frontend dev server (port 5173)
3. Electron вікно з додатком

**Примітка:** При першому запуску може зайняти 10-15 секунд.

---

## 📦 Збірка додатку

### Для Mac (твій M3):

```bash
npm run electron:build:mac
```

Створить:
- `dist-electron/Турнір-1.0.0-arm64.dmg` (для Apple Silicon M1/M2/M3)
- `dist-electron/Турнір-1.0.0-x64.dmg` (для Intel Mac)
- `dist-electron/Турнір-1.0.0-arm64-mac.zip`
- `dist-electron/Турнір-1.0.0-mac.zip`

### Для Windows:

```bash
npm run electron:build:win
```

Створить:
- `dist-electron/Турнір Setup 1.0.0.exe` (інсталятор)
- `dist-electron/Турнір 1.0.0.exe` (portable версія)

### Для Mac + Windows одночасно:

```bash
npm run electron:build:all
```

**Важливо:** Для збірки Windows на Mac потрібен Wine або збирати на Windows машині.

---

## 📱 Використання

### Після встановлення на Mac:

1. Відкрийте `Турнір.app` з теки Applications
2. Додаток автоматично запустить сервер
3. Відкриється головне вікно турніру
4. У tray bar (верхня панель) з'явиться іконка 🏆
5. Клік правою кнопкою на іконці → побачите IP адресу для підключення

### Після встановлення на Windows:

1. Запустіть `Турнір` з Start Menu або Desktop
2. Решта аналогічно Mac

---

## ✨ Можливості desktop додатку

### Порівняно з веб-версією:

✅ **Автоматичний запуск серверу** - не потрібно вручну запускати `npm run server`
✅ **Іконка в tray/taskbar** - швидкий доступ до IP адреси
✅ **Нативні повідомлення** - системні нотифікації
✅ **Авто-оновлення** - можливість додати в майбутньому
✅ **Один клік** - просто запустити .app або .exe
✅ **Мінімізація в tray** - не займає місце в dock/taskbar

### Функції tray menu:

- 📡 **Адреса для підключення** - показує IP адресу
- 📋 **Копіювати адресу** - одним кліком в буфер обміну
- 🪟 **Показати вікно** - відкриває головне вікно
- 🌐 **Відкрити в браузері** - відкриває в дефолтному браузері
- 🚪 **Вийти** - закриває додаток та зупиняє сервер

---

## 🎯 Workflow

### Для адміністратора:

1. **Встанови додаток** на свій Mac/PC
2. **Запусти** `Турнір.app`
3. **Клік правою** на іконку в tray → "Адреса для підключення"
4. **Копіюй адресу** (наприклад `http://192.168.1.100:5173`)
5. **Надішли** учасникам в групу

### Для учасників:

**Варіант 1:** Веб (як зараз)
- Відкрити адресу в браузері на телефоні/комп'ютері

**Варіант 2:** Нативний клієнт (опціонально)
- Встановити додаток собі на комп'ютер
- Просто відкрити його - підключиться автоматично

---

## 🔧 Технічні деталі

### Структура:

```
turik/
├── electron/
│   ├── main.cjs           # Головний процес Electron
│   ├── preload.cjs        # Preload скрипт
│   ├── icon.png           # PNG іконка
│   ├── icon.icns          # macOS іконка
│   └── icon.ico           # Windows іконка
├── server/                # Backend (запускається автоматично)
├── client/dist/           # Frontend (вбудований в додаток)
└── package.json           # Electron налаштування
```

### Що відбувається при запуску:

1. Electron запускає `electron/main.cjs`
2. Main process запускає `node server/index.js`
3. Очікує 2 секунди поки сервер готовий
4. Відкриває вікно з `client/dist/index.html`
5. Створює tray icon з меню
6. Відображає IP адресу в консолі та меню

### Збірка включає:

- ✅ Весь Node.js runtime
- ✅ Backend сервер + залежності
- ✅ Frontend build (статичні файли)
- ✅ Socket.io для real-time
- ✅ Всі node_modules

### Розмір:

- **Mac (arm64):** ~150-200 MB (DMG), ~300-400 MB (встановлений)
- **Windows:** ~120-180 MB (installer), ~250-350 MB (встановлений)

Це нормальний розмір для Electron додатків (наприклад VS Code ~300MB).

---

## 📝 Troubleshooting

### Помилка при збірці Mac:

```bash
# Встановіть Xcode Command Line Tools
xcode-select --install
```

### Помилка при збірці Windows на Mac:

Збірка Windows на Mac вимагає Wine. Краще збирати на Windows машині.

### Додаток не запускається:

1. Перевірте що порти 3000 та 5173 вільні
2. Спробуйте перезапустити
3. Подивіться логи: `Console.app` (Mac) або Event Viewer (Windows)

### Backend не стартує:

Додаток автоматично запускає backend. Якщо не працює:
- Перевірте чи є `server/index.js`
- Перевірте чи встановлені `node_modules`

---

## 🎁 Бонуси

### Додати власну іконку:

1. Створіть PNG 512x512px
2. Збережіть як `electron/icon.png`
3. Запустіть `./electron/create-icons.sh`
4. Пересоберіть додаток

### Змінити назву додатку:

В `package.json` → `build.productName`

### Додати автооновлення:

Використовуйте `electron-updater` (можна додати пізніше)

---

**Насолоджуйтесь нативним додатком! 🎉**

