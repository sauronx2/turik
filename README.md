# 🏆 Turik - Турнірна сітка з системою ставок

Інтерактивна турнірна сітка з real-time оновленнями, системою ставок, чатом та лідербордом для локальної мережі.

## Особливості

- 🎯 Турнірна сітка: 4 групи (по 3 гравці) → 1/4 фіналу → 1/2 фіналу → Фінал
- 🔐 Система реєстрації та автентифікації
- 💾 Автоматичне збереження сесії (не вилогінює після перезагрузки)
- 🍺 Система ставок з реальними виграшами/програшами
- 👑 Режим адміністратора з повним контролем
- ⚙️ Адмін-панель: скидання матчів, заміна гравців, управління ставками
- 💬 Чат для учасників з timestamp
- 📊 Публічний лідерборд
- 🔄 Real-time оновлення через WebSocket
- 📱 Мінімалістичний responsive дизайн (Google-style)
- 🌐 Підтримка локальної мережі

## Технології

- **Frontend**: React + Vite + TailwindCSS
- **Backend**: Node.js + Express + Socket.io
- **Real-time**: WebSocket
- **Storage**: localStorage для збереження сесії

## Швидкий старт

### 1. Встановлення залежностей

```bash
# Root dependencies
npm install

# Client dependencies
cd client
npm install
cd ..
```

### 2. Запуск серверу

```bash
npm run server
```

Сервер запуститься на `http://localhost:3000`

### 3. Запуск клієнта (в іншому терміналі)

```bash
cd client
npm run dev
```

Клієнт запуститься на `http://localhost:5173`

### 4. Підключення з інших пристроїв

Знайдіть IP вашого комп'ютера:
```bash
ifconfig | grep "inet "
```

На інших пристроях відкрийте: `http://YOUR_IP:5173`

## Використання

### 👤 Реєстрація учасника

1. Відкрийте додаток
2. Натисніть "Зареєструватись"
3. Введіть логін та пароль
4. Автоматично увійдете в систему
5. Отримайте 20 пляшок на старт 🍺

### 🔑 Вхід як адміністратор

```
Логін: admin
Пароль: Whitepower1488
```

### 🎮 Як адміністратор

**Основний режим:**
- Обирайте переможців на кожному етапі
- Турнір автоматично переходить до наступного раунду
- Ставки обробляються після кожного матчу

**Адмін-панель (⚙️ кнопка):**
- **Скинути матч** - відміняє результат будь-якого матчу
- **Замінити гравця** - замінює гравця на іншого по всьому турніру
- **Управління ставками** - перегляд і скасування ставок користувачів
- **Список користувачів** - перегляд балансу всіх учасників

### 🍺 Як учасник

**Ставки:**
- Робіть ставки на гравців (до 10 пляшок за раз)
- Максимальний баланс: 20 пляшок
- Виграйте пляшки, ставлячи на переможців
- Втратьте пляшки, ставлячи на тих, хто програв
- Скасувати ставку може тільки адміністратор

**Чат:**
- Спілкуйтесь з іншими учасниками
- Максимум 200 символів на повідомлення
- Історія останніх 100 повідомлень
- Адміністратор не бачить чат

**Лідерборд:**
- Топ-3 виділені кольорами 🥇🥈🥉
- Статус онлайн/офлайн
- Сортування за кількістю пляшок

## Логіка турніру

### Структура

```
Група A (3 гравці) ─→ переможець ─┐
                                    ├─→ 1/4 фіналу 1 ─┐
Група B (3 гравці) ─→ переможець ─┘                  │
                                                       ├─→ Півфінал ─→ Фінал → 🏆
Група C (3 гравці) ─→ переможець ─┐                  │
                                    ├─→ 1/4 фіналу 2 ─┘
Група D (3 гравці) ─→ переможець ─┘
```

### Гравці

**Група A**: Черняк Юрій, Костюк Артем, Мороз Олександр  
**Група B**: Денисюк Іван, Віка, Ройко Діма  
**Група C**: Назарук Богдан, Вітя, Тарас  
**Група D**: Дракон, Черняк Микола, Ліна

### Логіка ставок

1. **Початковий баланс**: 20 пляшок
2. **Ставка**: Оберіть гравця та кількість (1-10 пляшок)
3. **Виграш**: Якщо ваш гравець перемагає → ви отримуєте частку з пулу програвшого пропорційно вашій ставці
4. **Програш**: Якщо ваш гравець програє → ваші пляшки йдуть тим, хто поставив на переможця
5. **Скасування**: Тільки адміністратор може скасувати ставку

**Приклад:**
- Гравець A проти Гравця B
- На A поставили: User1 (5 🍺), User2 (3 🍺) = 8 🍺
- На B поставили: User3 (4 🍺) = 4 🍺
- **A перемагає:**
  - User1 отримує: 5 + (4 × 5/8) = 5 + 2.5 = 7 🍺
  - User2 отримує: 3 + (4 × 3/8) = 3 + 1.5 = 4 🍺
  - User3 втрачає свої 4 🍺

## Розробка

### Структура проекту

```
turik/
├── server/
│   └── index.js              # Backend з Socket.io
├── client/
│   ├── src/
│   │   ├── components/
│   │   │   ├── AuthScreen.jsx        # Логін/реєстрація
│   │   │   ├── TournamentBracket.jsx # Турнірна сітка
│   │   │   ├── BettingPanel.jsx      # Панель ставок
│   │   │   ├── Leaderboard.jsx       # Лідерборд
│   │   │   ├── Chat.jsx              # Чат
│   │   │   └── AdminPanel.jsx        # Адмін-панель
│   │   ├── App.jsx
│   │   └── main.jsx
│   └── package.json
└── package.json
```

### Налаштування

#### Змінити гравців

Відредагуйте `server/index.js`:

```javascript
groups: {
  A: { name: 'Група A', players: ['Гравець 1', 'Гравець 2', 'Гравець 3'], winner: null },
  // ...
}
```

#### Змінити початкову кількість пляшок

У `server/index.js`:

```javascript
registeredUsers[username] = {
  password,
  bottles: 20  // Змініть на бажану кількість
};
```

#### Змінити адмін-пароль

У `server/index.js`:

```javascript
const ADMIN_PASSWORD = 'YourNewPassword';
```

### Socket.io Events

**Client → Server:**
- `register` - реєстрація
- `login` - логін
- `place-bet` - зробити ставку
- `chat-message` - надіслати повідомлення
- `set-group-winner` - (admin) встановити переможця групи
- `set-quarterfinal-winner` - (admin) встановити переможця 1/4
- `set-semifinal-winner` - (admin) встановити переможця 1/2
- `set-final-winner` - (admin) встановити переможця фіналу
- `admin-reset-match` - (admin) скинути матч
- `admin-replace-player` - (admin) замінити гравця
- `admin-remove-bet` - (admin) скасувати ставку

**Server → Client:**
- `tournament-state` - стан турніру
- `active-bets` - активні ставки
- `users-list` - список користувачів
- `update-bottles` - оновлення балансу
- `chat-history` - історія чату
- `chat-message` - нове повідомлення

## Git

```bash
# Перегляд статусу
git status

# Перегляд історії
git log --oneline

# Додати remote
git remote add origin https://github.com/your-username/turik.git

# Push
git push -u origin main
```

## Безпека

⚠️ **Важливо:** Цей додаток призначений для використання у локальній мережі на приватних заходах. Не використовуйте в production без додаткових заходів безпеки:

- Паролі зберігаються у відкритому вигляді
- Немає rate limiting
- Немає HTTPS
- Немає захисту від CSRF/XSS

## Ліцензія

MIT
